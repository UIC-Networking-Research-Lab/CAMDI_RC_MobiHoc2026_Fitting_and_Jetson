# NVIDIA Jetson Orin Nano: L4T r36.4.0.
# Experiment Python environment: Python 3.12 (/opt/venv).
# Versions below match the reported experiment environment pip3 list.
# Use Jetson-compatible torch/torchvision builds for CUDA support.

# Inference and tokenization
torch==2.7.0
torchvision==0.22.0
transformers==5.3.0
huggingface_hub==1.6.0
tokenizers==0.22.2
safetensors==0.7.0
protobuf==6.30.2

# Optimization and accuracy fitting
numpy==2.2.4
scipy==1.17.1
pandas==3.0.1
scikit-learn==1.8.0
joblib==1.5.3
threadpoolctl==3.6.0

# HTTP transport
requests==2.32.3
Flask==3.1.3
Werkzeug==3.1.6

# Data loading, plots and progress
matplotlib==3.10.8
pillow==11.2.1
tqdm==4.67.1

# Optional tools absent from the reported environment (versions not recorded):
#   python -m pip install datasets       # GLUE download / SST-2 export
#   python -m pip install sentencepiece  # Tokenizer backends that require it
